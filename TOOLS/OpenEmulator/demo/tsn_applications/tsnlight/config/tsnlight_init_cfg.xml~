<?xml version="1.0" encoding="utf-8"?>
<network_init_cfg>
	<!-- 标签中的值全部用16进制主机序表示-->

	<!-- 节点0-->
	<tsnlight> 
		<tsnlight_mid>001</tsnlight_mid>   <!-- TSNLight的mid地址-->
	</tsnlight>	
	<!-- 节点0-->
	<node> 
		<hcp_mid>0</hcp_mid>   <!-- 交换机的本地dmid地址，16进制主机序-->
		<tsmp_forward_table>  <!-- 交换机的dmid转发表-->         
            <entry> <!-- 控制器转发表-->
				<dmid>8</dmid> 
				<outport>1</outport>  
			</entry>
			<entry> <!-- 同步应用主机转发表-->
				<dmid>c</dmid> 
				<outport>2</outport>  
			</entry> 	
			<entry> <!-- 同步应用主机转发表-->
				<dmid>4</dmid> 
				<outport>8</outport>  
			</entry> 	
			<entry> <!-- 同步应用主机转发表-->
				<dmid>10</dmid> 
				<outport>8</outport>  
			</entry> 		
			<entry> <!-- 同步应用主机转发表-->
				<dmid>14</dmid> 
				<outport>8</outport>  
			</entry>			
		</tsmp_forward_table>
	</node>

	<!-- 节点1-->
	<node> 
		<hcp_mid>4</hcp_mid>   <!-- 交换机的本地dmid地址，16进制主机序-->
		<tsmp_forward_table>  <!-- 交换机的dmid转发表-->         
            <entry> <!-- 控制器转发表-->
				<dmid>8</dmid> 
				<outport>8</outport>  
			</entry>
			<entry> <!-- 同步应用主机转发表-->
				<dmid>c</dmid> 
				<outport>8</outport>  
			</entry> 	
			<entry> <!-- 同步应用主机转发表-->
				<dmid>0</dmid> 
				<outport>8</outport>  
			</entry> 	
			<entry> <!-- 同步应用主机转发表-->
				<dmid>10</dmid> 
				<outport>1</outport>  
			</entry> 		
			<entry> <!-- 同步应用主机转发表-->
				<dmid>14</dmid> 
				<outport>2</outport>  
			</entry>	
		</tsmp_forward_table>
	</node>

	<!-- 节点2-->
	<node> 
		<hcp_mid>8</hcp_mid>   <!-- 交换机的本地dmid地址，16进制主机序-->
	</node>

	<node> 
		<hcp_mid>c</hcp_mid>   <!-- 交换机的本地dmid地址，16进制主机序-->
	</node>

	<node> 
		<hcp_mid>10</hcp_mid>   <!-- 交换机的本地dmid地址，16进制主机序-->
	</node>

	<node> 
		<hcp_mid>14</hcp_mid>   <!-- 交换机的本地dmid地址，16进制主机序-->
	</node>	
</network_init_cfg>


